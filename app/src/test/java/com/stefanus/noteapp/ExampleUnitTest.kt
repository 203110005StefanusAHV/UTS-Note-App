package com.stefanus.noteapp

import org.junit.Test

import org.junit.Assert.*

/**
 * untuk melakukan test ke development machine (host).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}